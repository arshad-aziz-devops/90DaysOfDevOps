Process management
  ps -ef --> Parent child relationshot. Who started the process?
              
  ps aux --> All process wth %CPU and %MEM field

  nohup and & --> run in backgroud and even if user logged out

  zz -tuln  --> display all listening ports

  lsof -i :port  ( which process using the port )

  vmstat
  iostat 
  top --> which process is high CPU/memory
  
File system
  wc
 mkdir -p --> creates directory
  
Networking troubleshooting
